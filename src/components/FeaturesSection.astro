---
import FeatureCard from './FeatureCard.astro';

export interface Props {
  title?: string;
}

const { title = 'Why merchants love us' } = Astro.props;

const features = [
  {
    icon: '🚀',
    title: 'Lightning Fast',
    description: 'Translate 50 products in under 5 minutes. No more waiting around.',
  },
  {
    icon: '🛡️',
    title: 'Brand Protection',
    description: 'Your brand names stay untouched. Always. Smart glossary keeps terms consistent.',
  },
  {
    icon: '💰',
    title: 'Transparent Pricing',
    description: 'Know exactly what you pay. No surprises, no hidden fees.',
  },
  {
    icon: '🎯',
    title: 'Quality Translation',
    description: 'E-commerce optimized prompts deliver natural, conversion-focused copy.',
  },
  {
    icon: '🔄',
    title: 'Auto Sync',
    description: 'Products update? Translations sync automatically. Set it and forget it.',
  },
  {
    icon: '💬',
    title: 'Human Support',
    description: "Real humans respond in under 4 hours. We're here when you need us.",
  },
];
---

<section id="features" class="section bg-white">
  <div class="container-wide px-4">
    <h2 class="text-h2 text-center mb-16">{title}</h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 lg:gap-12">
      {features.map((feature) => (
        <FeatureCard
          icon={feature.icon}
          title={feature.title}
          description={feature.description}
        />
      ))}
    </div>

    <!-- Optional slot for additional content -->
    <slot />
  </div>
</section>
